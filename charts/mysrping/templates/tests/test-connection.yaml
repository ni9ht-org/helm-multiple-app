apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysrping.fullname" . }}-test-connection"
  labels:
    {{- include "mysrping.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mysrping.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
