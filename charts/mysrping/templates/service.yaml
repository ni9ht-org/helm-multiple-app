apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysrping.fullname" . }}
  labels:
    {{- include "mysrping.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mysrping.selectorLabels" . | nindent 4 }}
